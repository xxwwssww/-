一、基本题（必须完成）

数据输入参考代码:

Scanner input = new Scanner(System.in);

double x=input.nextDouble();


二、基本题（必须完成）

（本课程的所有实验，都是在上周实验的JavaSE项目中添加新的包，然后一起压缩成JavaSE.rar上传提交）

在JavaSE中创建包lab2，然后完成以下题目：

1、在包lab2中创建一个名为Lab2_1的类，实现以下要求:

一个数如果恰好等于它的因子之和，这个数就称为“完数”。例如6的因子为1、2、3，而6=1+2+3。因此6是“完数”。编写一个小应用程序求1000之内的所有完数。

2、在包lab2中创建一个名为Lab2_2的类，实现以下要求:

输入某年某月某日，判断这一天是这一年的第几天?

3、在包lab2中创建一个名为Lab2_3的类，实现以下要求:

（2017南海区小学GOC测评题）

最长单调：

水塘边有一排N棵高度不相同的树，小P想用宽度为10的矩形表示他们。不过小C不喜欢表示树的这些图形有高低起伏，因此想找出高度是连续上升或连续下降的数量最多的树。

例如，N=7，树的高度为: 100 30 40 80 60 90 50 。其中(30 40 80)这一段是单调上升的，并且树的数量最多（3棵）。

参考输入：

7

100 30 40 80 60 90 50 

参考输出：

3

（数据规模：<10^7）
